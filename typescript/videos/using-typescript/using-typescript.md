First, you need to install TypeScript into your file system.
  In the termminal, type:
    npm init -y
    npm install typescript --save-dev
  This will create a package.json file and install TypeScript into your project.

