// 1) Write a JavaScript function called greet that takes a name as an argument and returns a greeting message. For example, if the name is "John", the function should return "Hello, John!".

/* interface Person {
    name:string;
}

const person = {
  name: "John",
};

function greet(person: Person): string {
  return `Hello, ${person.name}!`;
}

greet(person); */

// 2) Write a JavaScript function called multiply that takes two numbers as arguments and returns their product. Test the function with different inputs.

/* const numOne = prompt("Choose a number: ");
const numTwo = prompt("Choose a number: ");

function multiply(numOne: number, numTwo: number) {
  let product = numOne * numTwo;
  console.log(product);
};

multiply(2, 4); */

// 3) Create a JavaScript object called person with properties for name, age, and gender. Assign appropriate values to the properties and display the object on the console.

/* const person = {
  name: "Ellie",
  age: 15,
  gender: "female"
}

console.log(person); */

// 4) Create a JavaScript object called car with properties for make, model, and year. Assign appropriate values to the properties and display the object on the console.

/* const car = {
  make: "Toyota",
  model: "Accent",
  year: 2022
}

console.log(car); */

// 5) Write a JavaScript function called reverseString that takes a string as an argument and returns the reverse of the string. For example, if the input is "Hello", the function should return "olleH".

/* const str = prompt("Enter a word you would like reversed: ");

function reverseString(str: string) {
  return str.split('').reverse().join('');
}

console.log(reverseString('Methodical')); */

// 6) Write a JavaScript function called calculateArea that takes the radius of a circle as an argument and returns the area of the circle. Use the formula A = π * r^2.

/* interface rad{
  rad: number;
}

let rad = prompt("Enter the radius of your circle: ")
const pi = 3.14159265358979323846;

function calculateArea(){
  return (pi * rad * rad);
}

Area = calculateArea()

console.log("The area of your cicle is: " + Area) */

// 7) 